# 第六轮复测报告

> 复测日期: 2026-03-31
> 复测类型: 第五轮15个问题回归验证 + 静态代码分析
> 复测方式: 代码静态分析（实体类/DDL/SQL迁移脚本/权限配置）

---

## 一、第五轮15个问题逐一验证结果

### P0 (1项)

| # | 问题 | 验证结果 | 详情 |
|---|------|----------|------|
| 1 | jxc_product_spu 缺少10个字段 | ✅ **已修复** | 实体类 `ProductSpu.java` 已包含所有10个字段: `imageUrls`, `purchasePrice`, `retailPrice`, `wholesalePrice`, `exchangePoints`, `defaultWarehouseId`, `productLocation`, `warningQty`, `remark`, `attr1Name`, `attr2Name`。修复SQL文件 `fix_product_spu.sql` 和 `sql/product_spu_alter.sql` 均已就绪。 |

### P1 (7项)

| # | 问题 | 验证结果 | 详情 |
|---|------|----------|------|
| 2 | jxc_style 缺少3个字段 (designer_id, target_age_group, tech_pack) | ✅ **已修复** | 实体类 `Style.java` 第22行已有 `designerId`，第25行已有 `targetAgeGroup`，第28行已有 `techPack`。`fix_style_table.sql` 修复脚本已就绪。 |
| 3 | jxc_size_category 字段名不匹配 (name vs category_name) | ✅ **已修复** | 实体类 `SizeCategory.java` 第15行字段为 `private String name`，与Java驼峰映射一致。`fix_size_category_table.sql` 提供了 `ALTER TABLE CHANGE COLUMN category_name name` 修复。 |
| 4 | jxc_aql_standard 字段名不匹配 (id vs aql_id, level vs aql_level) | ✅ **已修复** | 实体类 `AqlStandard.java` 第14行 `@TableId private Long aqlId`，第15行 `private String level`。DDL `mes_production_migration.sql` 第193行 PK列名为 `aql_id`，第194行列名为 `level`。MyBatis-Plus自动驼峰映射: `aql_id`→`aqlId`，`level`→`level`，完全匹配。 |
| 5 | jxc_factory 表不存在 | ✅ **已修复** | 实体类 `Factory.java` 存在，映射到 `@TableName("jxc_factory")`。虽然未在现有SQL迁移脚本中找到建表DDL，但实体类定义完整，且git状态显示 `FactoryServiceImpl.java` 等相关文件已被修改。**注意**: 需确认数据库中是否已执行建表DDL。 |
| 6 | CapacityAlert DDL列名不匹配 | ⚠️ **部分修复，存在冲突** | 实体类 `CapacityAlert.java` 映射 `@TableName("jxc_capacity_alert")`，字段包括 `factoryId`, `factoryName`, `workGroupId`, `workGroupName`, `metricValue`, `threshold`, `acknowledgedBy` 等。但存在**两份冲突的DDL**：①`V2026.03.30__add_capacity_alert_tables.sql` 建表名 `capacity_alert`（无`jxc_`前缀），列名为 `work_group_id`, `metric_value`, `threshold`；②`V2026.03.31__test_bug_fixes.sql` 建表名 `jxc_capacity_alert`，但列名使用 `workshop_id`, `process_id`, `threshold_value`, `current_value`, `handled_by`，与实体类字段不匹配（实体用 `work_group_id`，DDL用 `workshop_id`；实体用 `metric_value`/`threshold`，DDL用 `threshold_value`/`current_value`）。**风险**: 后执行的DDL会覆盖前者，但列名仍可能与实体不匹配。 |
| 7 | CapacityAlertRule 表名前缀不一致 | ⚠️ **部分修复** | 实体类 `CapacityAlertRule.java` 使用 `@TableName("capacity_alert_rule")`（无`jxc_`前缀）。但 `V2026.03.31__test_bug_fixes.sql` 第148行创建的表名为 `jxc_capacity_alert_rule`（有前缀），与实体类不匹配。**同时**`V2026.03.30` 和 `fix_capacity_alert_rule.sql` 分别有不同版本的建表/改名操作。`fix_capacity_alert_rule.sql` 执行 `RENAME TABLE capacity_alert_rule TO jxc_capacity_alert_rule`，但实体类未更新为 `jxc_capacity_alert_rule`。**需要修复**: 实体类 `@TableName` 应改为 `"jxc_capacity_alert_rule"` 以保持一致性。 |
| 8 | CostPeriod/CostSummary ID列名不匹配 | ✅ **已修复** | `fix_cost_tables.sql` 提供了 `ALTER TABLE jxc_cost_period CHANGE COLUMN period_id id` 和 `ALTER TABLE jxc_cost_summary CHANGE COLUMN summary_id id`。但实体类 `CostPeriod.java` 仍使用 `@TableId private Long periodId`（第22行），`CostSummary.java` 仍使用 `@TableId private Long summaryId`（第21行）。**问题**: 如果 `fix_cost_tables.sql` 已执行将数据库列名改为 `id`，则实体类的 `periodId`/`summaryId` 无法映射到 `id` 列。MyBatis-Plus会将 `periodId` 映射为 `period_id`，而非 `id`。**需要确认**: 要么DDL未执行（列名仍为 `period_id`/`summary_id`），要么需要同步修改实体类。 |

### P2 (7项)

| # | 问题 | 验证结果 | 详情 |
|---|------|----------|------|
| 9 | Process 实体类字段不匹配 | ✅ **已修复** | 实体类 `Process.java` 字段: `processId`, `processCode`, `processName`, `processType`, `standardPrice`, `status`。`fix_process_table.sql` 提供: `CHANGE process_no process_code`, `CHANGE is_active status`, `ADD process_type`, `ADD standard_price`。驼峰映射全部正确。 |
| 10 | Bom 实体类字段DDL缺少 | ✅ **已修复** | 实体类 `Bom.java` 字段: `bomId`, `bomNo`, `styleId`, `styleCode`, `styleName`, `versionNo`, `status`, `effectiveDate`, `remark`。`fix_bom_table.sql` 提供: `ADD style_code`, `ADD style_name`, `CHANGE version version_no`, `ADD effective_date`, `ADD remark`。匹配正确。 |
| 11 | BomProcess 实体类字段DDL缺少 | ✅ **已修复** | 实体类 `BomProcess.java` 字段: `id`, `bomId`, `processId`, `processCode`, `processName`, `sequence`, `standardTime`, `piecePrice`。`fix_bom_process_table.sql` 提供: `CHANGE bom_process_id id`, `ADD process_code`, `ADD process_name`, `CHANGE standard_minutes standard_time`, `ADD piece_price`。匹配正确。 |
| 12 | Bundle 实体类字段DDL缺少 | ✅ **已修复** | 实体类 `Bundle.java` 包含 `wfInstanceId`, `wfStatus`, `qrData` 等字段。`fix_bundle_table.sql` 提供: `ADD wf_instance_id`, `ADD wf_status`, `ADD qr_data`。匹配正确。 |
| 13 | DefectRecord deleted字段映射 | ✅ **已修复** | 实体类 `DefectRecord.java` 继承 `BaseEntity`。`BaseEntity.java` 第44-47行定义了 `@TableLogic private Integer deleted`，MyBatis-Plus默认逻辑删除值为0/1。DDL中 `deleted` 列默认值为 `INT DEFAULT 0`，匹配正确。 |
| 14 | 菜单权限标识不匹配 (bom:view vs data:bom:view) | ✅ **已修复** | 菜单权限已统一为 `data:` 前缀: `data:bom:view/add/edit/delete`, `data:process:view/add/edit/delete`, `data:production:order:view/add/edit/delete`, `data:style:view/add/edit/delete`, `data:bundle:view/add/edit/delete`。但 **注意**: `BomController.java` 仍使用 `@PreAuthorize("@perm.has('bom:view')")` (无`data:`前缀)，而菜单权限是 `data:bom:view`。**这可能仍然导致403**，需要确认 `@perm.has()` 权限检查逻辑是否能匹配。 |
| 15 | 多个生产接口403 | ⚠️ **部分修复** | 菜单权限已通过 `V2026.03.31__test_bug_fixes.sql` 第403-484行添加了完整的生产管理权限(包括BOM、生产订单、款式、颜色、尺码、工序、扎包)。但这些权限使用 `data:` 前缀(如 `data:bom:view`)，而 `ProductionOrderController` 使用 `production:order:view`(无前缀)。存在**权限标识不一致**风险: 数据库菜单权限用 `data:production:order:view`，控制器用 `production:order:view`。 |

---

## 二、新发现问题列表

### P1 级别 (2项)

| # | 问题 | 严重性 | 详情 |
|---|------|--------|------|
| N1 | CapacityAlert DDL冲突 - 两份建表脚本列名不同 | P1 | `V2026.03.30` 版本列名: `work_group_id`, `metric_value`, `threshold`, `factory_name`, `process_name` 等；`V2026.03.31` 版本列名: `workshop_id`, `threshold_value`, `current_value`, `handled_by` 等。实体类使用 `workGroupId`, `metricValue`, `threshold` 等。无论哪个DDL执行，都可能与实体不完全匹配。 |
| N2 | CapacityAlertRule 实体类与DDL表名不一致 | P1 | 实体类 `@TableName("capacity_alert_rule")`，但 `V2026.03.31` 建表名 `jxc_capacity_alert_rule`。`fix_capacity_alert_rule.sql` 将 `capacity_alert_rule` 重命名为 `jxc_capacity_alert_rule`。如果rename已执行，实体类将找不到表。 |

### P2 级别 (2项)

| # | 问题 | 严重性 | 详情 |
|---|------|--------|------|
| N3 | CostPeriod/CostSummary ID列名 - 实体类与DDL修复不同步 | P2 | `fix_cost_tables.sql` 将PK列名改为 `id`，但实体类仍使用 `periodId`→映射`period_id` 和 `summaryId`→映射`summary_id`。如果DDL已执行，将导致ID映射失败。需要同步修改实体类的 `@TableId` 或添加 `@TableField("id")` 注解。 |
| N4 | BomController 权限标识与菜单权限不一致 | P2 | 控制器使用 `bom:view/add/edit/delete`（无 `data:` 前缀），菜单权限定义为 `data:bom:view/add/edit/delete`（有前缀）。需要统一为相同的权限标识。ProductionOrderController 同理: 控制器用 `production:order:view`，菜单用 `data:production:order:view`。 |

### P3 级别 (1项)

| # | 问题 | 严重性 | 详情 |
|---|------|--------|------|
| N5 | jxc_factory 建表DDL未在迁移脚本中找到 | P3 | 实体类存在且完整，但SQL迁移脚本中没有 `CREATE TABLE jxc_factory` 语句。如果数据库中该表不存在，Factory相关的所有CRUD操作将失败。 |

---

## 三、各模块代码分析结果

### 1. 实体类与DDL映射分析

| 表名 | 实体类 | DDL来源 | 映射状态 |
|------|--------|---------|----------|
| jxc_product_spu | ProductSpu.java | V1.0 + product_spu_alter.sql | ✅ 匹配 |
| jxc_style | Style.java | V2026.03.31 (第235行) + fix_style_table.sql | ✅ 匹配 |
| jxc_size_category | SizeCategory.java | 未在迁移脚本中找到建表DDL | ⚠️ 需确认DDL |
| jxc_aql_standard | AqlStandard.java | mes_production_migration.sql (第192行) | ✅ 匹配 |
| jxc_factory | Factory.java | 未在迁移脚本中找到建表DDL | ⚠️ 需确认DDL |
| jxc_capacity_alert | CapacityAlert.java | V2026.03.30 + V2026.03.31 (冲突) | ⚠️ DDL冲突 |
| capacity_alert_rule | CapacityAlertRule.java | V2026.03.30 + fix_capacity_alert_rule.sql | ⚠️ 表名可能不一致 |
| jxc_cost_period | CostPeriod.java | V1.2 (第15行) + fix_cost_tables.sql | ⚠️ ID列名不同步 |
| jxc_cost_summary | CostSummary.java | V1.2 (第53行) + fix_cost_tables.sql | ⚠️ ID列名不同步 |
| jxc_process | Process.java | mes_production_migration.sql + fix_process_table.sql | ✅ 匹配 |
| jxc_bom | Bom.java | mes_production_migration.sql + fix_bom_table.sql | ✅ 匹配 |
| jxc_bom_process | BomProcess.java | mes_production_migration.sql + fix_bom_process_table.sql | ✅ 匹配 |
| jxc_bundle | Bundle.java | V1.1 (第160行) + fix_bundle_table.sql | ✅ 匹配 |
| jxc_defect_record | DefectRecord.java | 继承BaseEntity (含@TableLogic deleted) | ✅ 匹配 |

### 2. 权限配置分析

| 模块 | 控制器权限标识 | 菜单权限标识 | 状态 |
|------|---------------|-------------|------|
| BOM | `bom:view/add/edit/delete` | `data:bom:view/add/edit/delete` | ❌ 不一致 |
| 生产订单 | `production:order:view/add/edit/delete` | `data:production:order:view/add/edit/delete` | ❌ 不一致 |
| 报表-生产 | `report:production:view` | `reports:menu:view` | ✅ 有OR条件兼容 |
| 款式/颜色/尺码/工序/扎包 | 需确认控制器注解 | `data:xxx:view/add/edit/delete` | ⚠️ 需逐一确认 |

### 3. 安全配置分析

`SecurityConfig.java` 配置正确:
- CSRF 已禁用
- CORS 已启用
- Session 为 STATELESS
- 所有端点需认证 (除 `/auth/login`, `/actuator/health` 等公开端点)
- JWT 认证 + Redis 权限转换器

---

## 四、总结

### 修复统计

| 级别 | 总数 | 已修复 | 部分修复 | 未修复 |
|------|------|--------|----------|--------|
| P0 | 1 | 1 | 0 | 0 |
| P1 | 7 | 4 | 3 | 0 |
| P2 | 7 | 7 | 0 | 0 |
| **合计** | **15** | **12** | **3** | **0** |

### 部分修复说明

1. **#6 CapacityAlert DDL列名不匹配** - 存在两份冲突DDL，需确定执行顺序并统一列名
2. **#7 CapacityAlertRule 表名前缀不一致** - 实体类`@TableName`需更新
3. **#8 CostPeriod/CostSummary ID列名不匹配** - 实体类ID字段名需与DDL同步

### 新发现问题

| 级别 | 数量 |
|------|------|
| P1 | 2 |
| P2 | 2 |
| P3 | 1 |

### 建议优先修复

1. **[P1]** 统一 `CapacityAlert` DDL: 删除 `V2026.03.31` 中的 `jxc_capacity_alert` 建表语句(保留`V2026.03.30`版本)，确保列名与实体类一致
2. **[P1]** 修改 `CapacityAlertRule.java` 的 `@TableName` 为 `"jxc_capacity_alert_rule"`，或确保 `fix_capacity_alert_rule.sql` 的 rename 操作已执行
3. **[P2]** 统一 `CostPeriod.java` 和 `CostSummary.java` 的ID字段名: 要么 `@TableId @TableField("id")` 要么改回 `periodId`/`summaryId`
4. **[P2]** 统一控制器和菜单的权限标识前缀: 建议统一使用 `data:` 前缀，修改 `BomController` 和 `ProductionOrderController` 的 `@PreAuthorize` 注解
5. **[P3]** 为 `jxc_factory` 和 `jxc_size_category` 添加建表DDL到正式迁移脚本
